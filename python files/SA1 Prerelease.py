import random

correct = 0
choice = int(input("Do you want to take the quiz? Say 1 for yes and 0 for no "))

if choice == 1:
	name = input("enter your name")
	length = int(input("how many questions?") 

	for i in range (0,length)
	    num1 = random.randint(2,12)
        num2 = random.randint(2,12)
		answer = num1 * num2
        response = int(input("What is", num1, "x", num2)
		if response == answer:
			correct +=1
		
	print (name,"your score is ", correct)

elif:
	break
		
	
	
